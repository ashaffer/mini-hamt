{
  "name": "mini-hamt",
  "description": "Minimal HAMT (Hash Array Mapped Trie) implementation for immutable maps",
  "repository": "git://github.com/ashaffer/mini-hamt.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/hash-str": "^1.0.0",
    "@f/popcount": "^1.0.0"
  },
  "devDependencies": {
    "@f/times": "^1.0.0",
    "babel-preset-es2015": "^6.1.2",
    "tape": "^4.2.0"
  }
}
